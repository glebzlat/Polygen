@entry
Grammar <- Text EOF

Text      <- (TextPiece? Rule)*
Rule      <- AT Name Newline
Name      <- "at"

# TextPiece <- (!(!'\\' '@') .)*
TextPiece <- (!AT .)*

AT <- !'\\' '@'

@ignore
Newline <- '\n'+

EOF <- !.
